// Posts manuais (opcional). O conteúdo do robô fica em content/publicados/.
// Para publicar uma matéria manualmente por aqui, siga o formato:
// { slug, category ("notícia" | "review" | "retrô"), nota (só reviews),
//   title, excerpt, author, date ("13 jul 2026"), readTime, cover: [cor1, cor2],
//   image (opcional), imageCredit (opcional), body: ["parágrafo 1", ...] }

export const posts = [];
